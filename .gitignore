# ── Docs / 프롬프트 ──────────────────────────────────
docs/

# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ── vpass-application 특정 ──────────────────────────
# 런타임 데이터 전체 제외 — 구명조끼 학습 데이터셋(data/jacket_dataset/)도 여기 포함된다.
# 학습 이미지는 git 에 올리지 않는다.
vpass-application/data/
# 학습된 TFLite 모델은 저장소 밖에서 따로 관리한다 (models/README.md 참고)
vpass-application/models/*.tflite

# ── Node / React UI ─────────────────────────────────
node_modules/
vpass-application/ui/dist/

# ── PlatformIO / 임베디드 빌드 산출물 ────────────────
.pio/
firmware/.pio/
firmware/include/config.h
kill-switch/.pio/

# ── OS / 에디터 ──────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.log

# ── TypeScript 빌드 캐시 ────────────────────────────
*.tsbuildinfo
