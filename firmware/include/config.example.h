#pragma once

// 이 파일을 config.h 로 복사한 뒤 본인 환경에 맞게 수정하세요.
//   cp config.example.h config.h   (Windows: copy config.example.h config.h)
#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// FastAPI 서버를 실행하는 PC 의 IP 주소와 포트.
// 예: PC IP 가 192.168.0.10 이면 "http://192.168.0.10:8000"
#define SERVER_URL    "http://192.168.0.10:8000"

// 이 구명조끼(사람)를 구분하는 이름. 서버 화면에 표시됩니다.
#define DEVICE_ID     "jacket-1"
